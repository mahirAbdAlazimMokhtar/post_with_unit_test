name: post_with_unit_test
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  bloc: ^8.1.2
 
  cupertino_icons: ^1.0.2
  dio: ^5.3.0
  flutter:
    sdk: flutter
  mockito: ^5.4.2

dev_dependencies:
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter

